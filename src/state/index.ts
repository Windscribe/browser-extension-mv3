import store, { type RootState, type AppDispatch, type StoreType, type GetState } from './store'
import { initializeWrappedStore } from './webExtAdapter'

export {
  store,
  type RootState,
  type AppDispatch,
  type StoreType,
  type GetState,
  initializeWrappedStore,
}
