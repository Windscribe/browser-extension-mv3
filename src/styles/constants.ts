import { keyframes } from '@emotion/react'

export const bodyMaxHeight = '442px'
export const unreadIconRadius = 4

export const SpinAnim = keyframes`
from {
  transform: rotate(0deg);
}
to {
  transform: rotate(360deg);
}
`
