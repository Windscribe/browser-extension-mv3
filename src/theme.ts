import type { Theme } from 'theme-ui'

export const theme: Theme = {
  config: {
    initialColorModeName: 'dark',
  },
  rawColors: {
    white: '#fff',
    text: '#000',
    green: '#55ff8a',
    modes: {
      light: {
        text: '#fff',
      },
    },
  },
}
